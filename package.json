{
  "name": "piphex-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Secure Piphex chat and voice backend",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check book-sources.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
